# Commits Created

This file is finalized after the release commits exist. Commit hashes are listed in
oldest-to-newest order. The SHA of the commit containing this report is resolved with
`git rev-parse HEAD` and is also supplied verbatim in the release-manager handoff; a Git
commit cannot embed its own SHA because changing the file changes that SHA.

