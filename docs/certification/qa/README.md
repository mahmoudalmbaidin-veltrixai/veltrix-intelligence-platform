# QA certification archive

`legacy/reports/` contains selected earlier remediation narratives. Raw machine output, screenshots, generated exports, browser reports, and local-only evidence are archived under ignored `artifacts/` rather than retained in the source tree.

Evidence is immutable context for the SHA named inside it, not a current pass. Rerun the current tests and repository security audit for every release candidate.
