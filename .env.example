# VIP frontend environment configuration
# Copy to `.env` (or `.env.local`) and adjust. Values are validated at startup
# by src/shared/config/env.ts — invalid config fails fast (see failSafe rules).

# Data source: 'mock' (default, no backend) or 'live' (requires VITE_API_BASE_URL)
VITE_API_MODE=mock

# Base URL for the live API. REQUIRED when VITE_API_MODE=live.
VITE_API_BASE_URL=

# Request timeout in milliseconds (default 20000)
VITE_API_TIMEOUT_MS=20000

# App environment: development | staging | production
VITE_APP_ENV=development

# Enable in-app dev tooling (role switcher etc.). Default: true in dev.
VITE_ENABLE_DEVTOOLS=true

# Simulate network latency in mock mode. Default: true.
VITE_ENABLE_MOCK_LATENCY=true
